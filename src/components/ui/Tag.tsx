export default function Tag({ children }: { children: React.ReactNode }) {
    return (
        <div className="font-bold bg-[#0000004D] py-3 px-[24px] rounded-lg w-fit h-fit">{children}</div>

    )
}